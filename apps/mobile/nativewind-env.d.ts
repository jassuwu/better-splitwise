/// <reference types="nativewind/types" />
/// <reference types="expo/types" />
// expo/types declares the `*.css` side-effect module (and more). It normally
// arrives via the auto-generated expo-env.d.ts, but that file is git-ignored,
// so reference it here too — otherwise type-check fails on a clean checkout (CI).
